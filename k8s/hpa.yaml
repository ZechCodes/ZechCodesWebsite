apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: zechcodes-website
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: zechcodes-website
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
